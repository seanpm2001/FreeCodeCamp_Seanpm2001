function convertToInteger(str) {
  // return parseInt(str, radix);
  // radix is undefined
  // return parseInt(string, radix);
  return parseInt(str, 2); // Base 2 radix
  // return parseInt(string, 2); // Base 2 radix
  // It would again be helpful if this was explained a bit better in the description, I had to get a hint again, as I didn't know where to go.
}

// convertToInteger = parseInt("10011", radix);
// convertToInteger = parseInt("10011");
convertToInteger("10011"); // Don't chang ethis