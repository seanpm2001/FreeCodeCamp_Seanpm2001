// Setup
const myArray = [];

// Only change code below this line
// x = 0;
for (let y = 1; y < 6; y++) { // It feels weird putting let inside a for loop
  myArray.push(y);
  // y > 6 y > 5 y = 0 y = 1 y = -1 = false
} // Note to self: do not EVER put => inside a for or while loop in this circumstance