function convertToInteger(str) {
  // var b = parseInt("056")
  // parseInt("056");
  return parseInt(str); // Why was this not included in the description? I had almost no idea what I was supposed to do without getting a hint.
}

// convertToInteger = parseInt("56");
convertToInteger("56");