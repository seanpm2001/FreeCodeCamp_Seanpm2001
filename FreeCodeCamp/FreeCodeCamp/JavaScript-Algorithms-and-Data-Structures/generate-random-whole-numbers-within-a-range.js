function randomRange(myMin, myMax) {
  /*var myMax = 5;
  var myMin = 1;*/
  var aRandomNumber = (1, 9)
  return Math.floor(Math.random() * (myMax - myMin + 1)) + myMin;
}
// Capital letters in the beginning of string = special keyword
// Do not define as max = 5, min = 1, define as myMax = 5, myMin = 
// Need to define different numbers